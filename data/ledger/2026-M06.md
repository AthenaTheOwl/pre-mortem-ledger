# Ledger row — 2026-M06 (initialization)

This is the first ledger row. It records that the v0.1 scaffold of the
Pre-Mortem Ledger went live on 2026-06-22 and that the worked example for
position `EXAMPLE` validated against the schema, the rubric, and the CLI.

## What ran

- The schema in `schemas/premortem.schema.json` was registered.
- The rubric in `rubric/failure_modes.yaml` was seeded with 10 categories.
- `examples/premortem-EXAMPLE-2026-M07.md` was authored as a worked
  example with three failure modes, each tagged to an existing rubric
  category and each carrying at least one observability trigger.
- `scripts/validate_premortem_schema.py examples/premortem-EXAMPLE-2026-M07.md`
  exits zero. `python -m pytest` exits zero.

## What this run did not do

- No real position was scored. The book is intentionally not named in
  this v0.1 row; the next monthly run is when `config/positions.yaml`
  acquires the first real ticker.
- No back-scoring (`prior_run_brier` is null). The first calibration
  delta lands in the second monthly run, after `2026-M07` failure modes
  have a `status_log` entry to compare against.

## Companion JSONL row

The machine-readable companion is the single row in `data/ledger/runs.jsonl`
with `run_id = "2026-M06-initialization"`. Future runs append; existing
rows are never edited. The append-only guarantee is enforced by
`pre_mortem_ledger.ledger.append_ledger`.
